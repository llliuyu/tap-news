import { RenderMime } from 'jupyter-js-ui/lib/rendermime';
import { IListChangedArgs, ObservableList } from 'phosphor-observablelist';
import { Message } from 'phosphor-messaging';
import { Widget } from 'phosphor-widget';
import { nbformat } from '../notebook/nbformat';
import { ObservableOutputs } from './model';
/**
 * An output area widget.
 */
export declare class OutputAreaWidget extends Widget {
    /**
     * Construct an output area widget.
     */
    constructor(outputs: ObservableOutputs, rendermime: RenderMime<Widget>);
    /**
     * The trusted state of the widget.
     */
    trusted: boolean;
    /**
     * The collapsed state of the widget.
     */
    collapsed: boolean;
    /**
     * The fixed height state of the widget.
     */
    fixedHeight: boolean;
    /**
     * Dispose of the resources held by the widget.
     */
    dispose(): void;
    /**
     * Create an output node from an output model.
     */
    protected createOutput(output: nbformat.IOutput): Widget;
    /**
     * Follow changes to the outputs list.
     */
    protected outputsChanged(sender: ObservableList<nbformat.IOutput>, args: IListChangedArgs<nbformat.IOutput>): void;
    /**
     * Handle `update_request` messages.
     */
    protected onUpdateRequest(msg: Message): void;
    private _sanitized;
    private _trusted;
    private _fixedHeight;
    private _collapsed;
    private _outputs;
    private _rendermime;
}
