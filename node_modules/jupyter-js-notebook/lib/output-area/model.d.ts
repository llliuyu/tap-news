import { IKernel, IExecuteReply } from 'jupyter-js-services';
import { ObservableList } from 'phosphor-observablelist';
import { nbformat } from '../notebook/nbformat';
/**
 * An observable list that handles output area data.
 */
export declare class ObservableOutputs extends ObservableList<nbformat.IOutput> {
    /**
     * Add an output, which may be combined with previous output
     * (e.g. for streams).
     */
    add(output: nbformat.IOutput): number;
    /**
     * Clear all of the output.
     *
     * @param wait Delay clearing the output until the next message is added.
     */
    clear(wait?: boolean): nbformat.IOutput[];
    private _clearNext;
}
/**
 * Execute code on a kernel and send outputs to an observable output.
 */
export declare function executeCode(code: string, kernel: IKernel, outputs: ObservableOutputs): Promise<IExecuteReply>;
