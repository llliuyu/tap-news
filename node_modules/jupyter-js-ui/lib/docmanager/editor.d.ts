import 'codemirror/mode/meta';
import { IKernelId } from 'jupyter-js-services';
import { CodeMirrorWidget } from '../codemirror/widget';
import { IDocumentModel, IWidgetFactory, IDocumentContext } from './index';
import { ABCWidgetFactory } from './default';
/**
 * A document widget for codemirrors.
 */
export declare class EditorWidget extends CodeMirrorWidget {
    /**
     * Construct a new editor widget.
     */
    constructor(model: IDocumentModel, context: IDocumentContext);
}
/**
 * A widget factory for editors.
 */
export declare class EditorWidgetFactory extends ABCWidgetFactory implements IWidgetFactory<EditorWidget> {
    /**
     * Create a new widget given a document model and a context.
     */
    createNew(model: IDocumentModel, context: IDocumentContext, kernel?: IKernelId): EditorWidget;
}
