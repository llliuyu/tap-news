import { IKernelId } from 'jupyter-js-services';
import { Message } from 'phosphor-messaging';
import { Widget } from 'phosphor-widget';
import { IDocumentModel, IWidgetFactory, IDocumentContext } from './index';
import { ABCWidgetFactory } from './default';
/**
 * A widget for images.
 */
export declare class ImageWidget extends Widget {
    /**
     * Create the node for the image widget.
     */
    static createNode(): HTMLElement;
    /**
     * Construct a new image widget.
     */
    constructor(model: IDocumentModel, context: IDocumentContext);
    /**
     * Dispose of the resources used by the widget.
     */
    dispose(): void;
    /**
     * Handle `update-request` messages for the widget.
     */
    protected onUpdateRequest(msg: Message): void;
    private _model;
    private _context;
}
/**
 * A widget factory for images.
 */
export declare class ImageWidgetFactory extends ABCWidgetFactory implements IWidgetFactory<ImageWidget> {
    /**
     * Create a new widget given a document model and a context.
     */
    createNew(model: IDocumentModel, context: IDocumentContext, kernel?: IKernelId): ImageWidget;
}
