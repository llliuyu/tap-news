import { DocumentManager, DocumentWidget } from '../docmanager';
import { FileBrowserModel } from './model';
/**
 * Open a file using a dialog.
 */
export declare function openWithDialog(path: string, manager: DocumentManager, host?: HTMLElement): Promise<DocumentWidget>;
/**
 * Create a new file using a dialog.
 */
export declare function createNewDialog(model: FileBrowserModel, manager: DocumentManager, host?: HTMLElement): Promise<DocumentWidget>;
